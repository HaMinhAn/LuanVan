spring:
  application:
    name: Backet-service
  datasource:
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost\MSI:1433;databaseName=BacketDB;encrypt=true;trustServerCertificate=true;
    username: sa
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9002
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
